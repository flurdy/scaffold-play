# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

POST    /registation/start                 controllers.RegistrationAuthentication.startRegistrationProcess
POST    /registation                 controllers.RegistrationAuthentication.finishRegistrationProcess
POST    /login                 controllers.RegistrationAuthentication.loginProcess

GET     /webjars/*file              controllers.WebJarAssets.at(file)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
